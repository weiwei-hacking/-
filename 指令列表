符號說明
{為變數必填}
(為變數選填)

/幫助
你可以透過這個指令獲取到機器人的信息

/身分組 新增全部 {role @身份組} (例外身分組 @身分組) (例外機器人 True/False)
你可以透過這個指令為伺服器中的所有用戶給予指定的身分組，且你可以透過變數來排除已有特定身分組的用戶或機器人

/身分組 移除全部} {role @身份組} (例外身分組 @身分組) (例外機器人 True/False)
你可以透過這個指令為伺服器中的所有用戶移除指定的身分組，且你可以透過變數來排除已有特定身分組的用戶或機器人

/資訊 用戶 (member @用戶)
你可以透過這個指令來查詢指定用戶或自己的基礎資訊 (用戶名稱、用戶ID、帳號創建日期 以及 加入伺服器的日期 等)

/資訊 伺服器
你可以透過這個指令來查詢目前所在的伺服器基礎資訊 (伺服器名稱、伺服器ID、建立時間、擁有 者以及 總成員數 等)

/踢出 {member @用戶}
你可以透過這個指令來踢出指定的用戶

/停權 {member @用戶}
你可以透過這個指令來停權指定的用戶

/禁言 {member @用戶} {秒數}
你可以透過這格指令來禁言指定用戶，並且可以設定禁言時間 (用秒數設定)

/解除停權 {user_id 用戶ID}
你可以透過這格指令來解除被停權的指定用戶

/解除禁言 {member @用戶}
你可以透過這格指令來解除被禁言的指定用戶

/重建
你可以透過這格指令來把當前的頻道刪除並建立一個相同設定的頻道
